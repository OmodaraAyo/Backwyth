import React from "react";
import { keyFeaturesData } from "../assets/MultiListView";

const KeyFeatures = () => {
  return (
    <section id="key-features">
      {/* <h1>key features</h1>
      <div>
        {keyFeaturesData.map((data, index) => (
          <div key={index}>
            <h1>{data.title}</h1>
            <h2>{data.details}</h2>
          </div>
        ))}
      </div> */}
    </section>
  );
};

export default KeyFeatures;
